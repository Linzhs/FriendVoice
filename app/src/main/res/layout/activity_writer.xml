<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:custom="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/write_board_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light">

        <TextView
            android:id="@+id/manual_toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/write_board_toolbar_title"
            android:textColor="@color/white"
            android:textSize="@dimen/toolbar_title_size"/>

    </android.support.v7.widget.Toolbar>

    <com.linzh.android.newfriendvoice.ui.custom.WriteBoardView
        android:id="@+id/writing_board"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        custom:paintColor="@color/colorAccent"
        custom:boardBackground="@color/colorPrimary"
        custom:paintWidth="2dp" />

</LinearLayout>
